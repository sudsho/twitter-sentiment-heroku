Flask>=1.1.1
vaderSentiment>=3.3.2
textblob>=0.15.3
PyYAML>=5.3.1
python-dotenv>=0.13.0
gunicorn>=20.0.4
pytest>=5.4.3
# tweepy is only needed for the live Twitter stream worker (src/stream.py).
# The offline smoke and the scoring/predict path do not import it.
tweepy>=3.8.0
